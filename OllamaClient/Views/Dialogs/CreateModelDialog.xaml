<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Dialogs.CreateModelDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    MinWidth="500">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Margin="10,0,10,0">New model name:</TextBlock>
            <TextBox x:Name="ModelNameTextBox" TextChanged="ModelNameTextBox_TextChanged" Template="{StaticResource DefaultInputTextBoxControlTemplate}"  />
        </StackPanel>
        <SelectorBar Grid.Row="1" x:Name="NavigationSelectorBar" SelectionChanged="NavigationSelectorBar_SelectionChanged">
            <SelectorBarItem x:Name="FromSelectorBarItem" Text="From" />
            <SelectorBarItem x:Name="TemplateSelectorBarItem" Text="Template" />
            <SelectorBarItem x:Name="ParametersSelectorBarItem" Text="Parameters" />
            <SelectorBarItem x:Name="SystemSelectorBarItem" Text="System" />
        </SelectorBar>
        <Frame Grid.Row="2" x:Name="ContentFrame" IsNavigationStackEnabled="False" />
    </Grid>
</Page>
