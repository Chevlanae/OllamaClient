<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Dialogs.ParametersContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20,10,20,10">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">Enter parameters (optional):</TextBlock>
        <ListView x:Name="ParametersListView"  Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Key}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" 
                                 x:Name="ParameterInputTextBox" 
                                 Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Template="{StaticResource DefaultInputTextBoxControlTemplate}" 
                                 MaxWidth="200"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <DropDownButton ToolTipService.ToolTip="Add new parameters" Margin="0,0,20,0">
                <FontIcon Glyph="&#xE710;" />
                <DropDownButton.Flyout>
                    <MenuFlyout x:Name="AddButtonMenuFlyout" Placement="Bottom" />
                </DropDownButton.Flyout>
            </DropDownButton>
            <Button ToolTipService.ToolTip="Clear parameters">
                <FontIcon Glyph="&#xE894;" />
            </Button>
        </StackPanel>
    </Grid>
</Page>
