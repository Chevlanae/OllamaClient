<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Dialogs.FromContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Margin="10,0,10,0">New name:</TextBlock>
            <TextBox x:Name="ModelNameTextBox" TextChanged="ModelNameTextBox_TextChanged" MaxWidth="160" MaxHeight="20" Template="{StaticResource DefaultInputTextBoxControlTemplate}"  />
        </StackPanel>
        <RelativePanel Grid.Column="2" HorizontalAlignment="Center">
            <TextBlock x:Name="LabelTextBox" RelativePanel.AlignLeftWithPanel="True" Margin="10,15,0,0" >
                Base model
            </TextBlock>
            <ComboBox x:Name="SelectedModelComboBox" RelativePanel.RightOf="LabelTextBox" Margin="10" SelectionChanged="SelectedModelComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </RelativePanel>
    </Grid>
</Page>
