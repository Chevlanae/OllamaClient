<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Pages.FromContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorTertiary}">

    <Grid>
        <Grid MaxWidth="300" MinHeight="100" MaxHeight="100" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="10,0,10,0">New name:</TextBlock>
            <TextBox 
                x:Name="ModelNameTextBox"
                TextChanged="ModelNameTextBox_TextChanged" 
                Grid.Row="0" 
                Grid.Column="1"
                MaxWidth="160" 
                MaxHeight="20" 
                Template="{StaticResource DefaultInputTextBoxControlTemplate}"  />
            <TextBlock 
                Grid.Row="1" Grid.Column="0"
                x:Name="LabelTextBox" 
                Margin="10,15,0,0" >
                Base model
            </TextBlock>
            <ComboBox 
                Grid.Row="1" Grid.Column="1"
                x:Name="SelectedModelComboBox"
                HorizontalAlignment="Center"
                Margin="10" 
                SelectionChanged="SelectedModelComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </Grid>
    </Grid>
</Page>
