<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <Grid>
            <Grid VerticalAlignment="Center" HorizontalAlignment="Stretch" MaxWidth="400" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" TextWrapping="Wrap" Text="Socket Address" />
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SocketAddress, Mode=TwoWay}" />
                <TextBlock Grid.Row="1" Grid.Column="0" TextWrapping="Wrap" Text="Use HTTPS" />
                <ToggleSwitch Grid.Row="1" Grid.Column="1" IsOn="{Binding UseHttps, Mode=TwoWay}" />
                <TextBlock Grid.Row="2" Grid.Column="0" TextWrapping="Wrap" Text="Request Timeout" />
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding RequestTimeout}"/>
                <TextBlock Grid.Row="3" Grid.Column="0" TextWrapping="Wrap" Text="Enable Model Parameters in Chat" />
                <ToggleSwitch Grid.Row="3" Grid.Column="1" IsOn="{Binding EnableModelParametersForChat}" />
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
