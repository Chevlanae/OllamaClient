<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OllamaClient.Views.Pages.ParametersContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OllamaClient.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SolidBackgroundFillColorTertiary}">

    <Grid Margin="20,10,20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">Enter parameters (optional):</TextBlock>
        <ListView x:Name="ParametersListView" MaxWidth="400" Margin="0,10,0,0"  Grid.Row="1" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Key}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBox Grid.Column="1" 
                                 x:Name="ParameterInputTextBox" 
                                 Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Template="{StaticResource DefaultInputTextBoxControlTemplate}" 
                                 MaxWidth="200"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel 
            Grid.Row="2" 
            MinHeight="50" 
            Orientation="Horizontal" 
            VerticalAlignment="Bottom"
            HorizontalAlignment="Right">
            <DropDownButton ToolTipService.ToolTip="Add new parameters" Margin="0,0,20,0">
                <FontIcon Glyph="&#xE710;" />
                <DropDownButton.Flyout>
                    <MenuFlyout x:Name="AddButtonMenuFlyout" Placement="Bottom" />
                </DropDownButton.Flyout>
            </DropDownButton>
            <Button x:Name="ClearParametersButton" ToolTipService.ToolTip="Clear parameters" Click="ClearParametersButton_Click">
                <FontIcon Glyph="&#xE894;" />
            </Button>
        </StackPanel>
    </Grid>
</Page>
